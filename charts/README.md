# Helm charts

The Helm charts are no longer maintained in this repository.

Use the dedicated upstream charts repository instead:
- <https://github.com/karakeep-app/helm-charts>

If this fork ever needs fork-specific charts, they should live in a separate chart repo rather than reintroducing them here.
